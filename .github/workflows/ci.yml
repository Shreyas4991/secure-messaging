name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
        # with:
          # Re-verify all declarations with an independent minimal type checker,
          # guarding against soundness bugs in the Lean elaborator
          # leanchecker: true
